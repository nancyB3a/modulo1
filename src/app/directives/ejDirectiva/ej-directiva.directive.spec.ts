import { EjDirectivaDirective } from './ej-directiva.directive';

describe('EjDirectivaDirective', () => {
  it('should create an instance', () => {
    const directive = new EjDirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
